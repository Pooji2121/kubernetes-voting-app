apiVersion: v1
kind: Service
metadata:
  name: voting-frontend-service
  labels:
    app: voting-app
spec:
  selector:
    app: voting-app
    tier: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
