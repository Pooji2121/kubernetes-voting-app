apiVersion: v1
kind: Service
metadata:
  name: voting-api-service
  labels:
    app: voting-app
spec:
  selector:
    app: voting-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
